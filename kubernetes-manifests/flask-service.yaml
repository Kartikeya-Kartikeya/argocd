apiVersion: v1
kind: Service
metadata:
  name: flask-service
  namespace: demo
  labels:
    app: flask-app
spec:
  type: LoadBalancer
  selector:
    app: flask-app
  ports:
    - name: http
      port: 80
      targetPort: 5000